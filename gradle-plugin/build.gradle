plugins {
    id "java-gradle-plugin"
    id "maven-publish"
    id "org.jetbrains.kotlin.jvm"
}

group = "ru.petproject.saved-state"
version = "1.0.0"

repositories {
    mavenCentral()
}

gradlePlugin {
    plugins {
        savedStatePlugin {
            id = "ru.petproject.saved-state-gradle-plugin"
            implementationClass = "ru.petproject.saved.state.gradle.SavedStateGradlePlugin"
            displayName = "SavedState Gradle Plugin"
            description = "Plugin provide compiler plugin and implement lib with annotation"
        }
    }
}

publishing {
    repositories {
        maven {

        }
    }
}

/*pluginBundle {
    plugins {
        savedStatePlugin {
            displayName = "SavedState Gradle Plugin"
            version = "1.0.0"
        }
    }

    mavenCoordinates {
        groupId = "ru.petproject"
        artifactId = "saved-state-gradle-plugin"
        version = "1.0.0"
    }
}*/

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib:$kotlin_version"
    implementation "org.jetbrains.kotlin:kotlin-gradle-plugin-api:$kotlin_version"
    compileOnly "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
}
